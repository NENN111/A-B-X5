# Промежуточные данные

Здесь хранятся воспроизводимые артефакты очистки и валидации, не являющиеся исходными данными. Содержимое каталога исключено из Git, кроме этой инструкции.

Stage 2 создаёт подкаталоги `clean/` с Parquet-файлами и `quality/` с raw/cleaned-профилями и аудитом преобразований. Форматы описаны в `docs/stage_02_data_cleaning.md`.

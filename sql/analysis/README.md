# Аналитические SQL-запросы

`01_quality_checks.sql` возвращает количество нарушений ссылочной и бизнес-целостности. Dashboard использует контролируемые витрины и не дублирует сложную статистическую логику в SQL или DAX.
